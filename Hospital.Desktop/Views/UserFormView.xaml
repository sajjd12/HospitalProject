<Window x:Class="Hospital.Desktop.Views.UserFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة بيانات المستخدم" Height="600" Width="400"
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="اسم المستخدم:" Margin="0,5"/>
            <TextBox Text="{Binding User.UserName}" IsEnabled="{Binding IsAddMode}" Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="الاسم الكامل:" Margin="0,5"/>
            <TextBox Text="{Binding User.FullName}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="الصلاحية:" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding User.Role}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Margin="0,0,0,10"/>

            <StackPanel Visibility="{Binding IsAddMode, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="كلمة المرور:" Margin="0,5"/>
                <PasswordBox x:Name="txtPassword" Padding="5" Margin="0,0,0,10"/>
            </StackPanel>

            <Separator Margin="0,10"/>

            <CheckBox Content="ربط بموظف" IsChecked="{Binding LinkEmployee}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding Employees}" DisplayMemberPath="FullName" SelectedValuePath="Id" 
                      SelectedValue="{Binding User.EmployeeId}" IsEnabled="{Binding LinkEmployee}" Margin="0,5,0,15"/>

            <UniformGrid Columns="2" Margin="0,10">
                <CheckBox Content="حساب نشط" IsChecked="{Binding User.IsActive}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}"/>
                <CheckBox Content="منقول للأرشيف" IsChecked="{Binding User.IsDeleted}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}"/>
            </UniformGrid>

            <Separator Margin="0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="حفظ" Command="{Binding SaveCommand}" CommandParameter="{Binding ElementName=txtPassword}"
                        Visibility="{Binding IsViewMode, Converter={StaticResource InverseBoolToVis}}"
                        Width="120" Height="35" Background="#2ECC71" Foreground="White" BorderThickness="0" Cursor="Hand"/>

                <Button Content="إلغاء / إغلاق" IsCancel="True" Width="120" Height="35" Margin="10,0,0,0" Cursor="Hand"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>