<Window x:Class="Hospital.Desktop.Views.UserFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة بيانات المستخدم" 
        Height="720" Width="480"
        WindowStartupLocation="CenterScreen" 
        FlowDirection="RightToLeft"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="ModernInput" TargetType="Control">
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10,0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="إدارة حسابات المستخدمين" FontSize="20" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="تعديل بيانات الدخول وصلاحيات النظام" FontSize="12" Foreground="#94A3B8" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel>
                <Border Background="White" CornerRadius="12" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.05" Color="Black"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="اسم المستخدم (Login Name)" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding User.UserName}" IsEnabled="{Binding IsAddMode}" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                        <TextBlock Text="الاسم الكامل" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding User.FullName}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                        <TextBlock Text="مستوى الصلاحية" Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding User.Role}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                        <StackPanel Visibility="{Binding IsAddMode, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="كلمة المرور" Style="{StaticResource FieldLabel}"/>
                            <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>
                        </StackPanel>

                        <Separator Margin="0,10,0,20" Background="#F1F5F9"/>

                        <CheckBox Content="ربط هذا الحساب بموظف من القائمة" IsChecked="{Binding LinkEmployee}" 
                                  IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" 
                                  Foreground="#334155" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <StackPanel Visibility="{Binding LinkEmployee, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="البحث عن موظف لربطه باللقب أو الاسم" FontSize="11" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Employees}" 
                                      DisplayMemberPath="Name" 
                                      SelectedValuePath="Id" 
                                      SelectedValue="{Binding User.EmployeeId}" 
                                      Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="True"
                                      IsTextSearchEnabled="False" 
                                      StaysOpenOnEdit="True"
                                      Style="{StaticResource ModernInput}"
                                      IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}">
                                <ComboBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel />
                                    </ItemsPanelTemplate>
                                </ComboBox.ItemsPanel>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="#F1F5F9" CornerRadius="10" Padding="15" Margin="0,20,0,0">
                    <UniformGrid Columns="2">
                        <CheckBox Content="الحساب نشط حالياً" IsChecked="{Binding User.IsActive}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Foreground="#166534" FontWeight="SemiBold"/>
                        <CheckBox Content="نقل إلى الأرشيف" IsChecked="{Binding User.IsDeleted}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBool}}" Foreground="#991B1B" FontWeight="SemiBold"/>
                    </UniformGrid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,25,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="حفظ التغييرات" 
                    Command="{Binding SaveCommand}" 
                    CommandParameter="{Binding ElementName=txtPassword}"
                    Visibility="{Binding IsViewMode, Converter={StaticResource InverseBoolToVis}}"
                    Style="{StaticResource PrimaryBtn}" 
                    Margin="0,0,12,0"/>

            <Button Grid.Column="1" 
                    Content="إغلاق" 
                    IsCancel="True" 
                    Style="{StaticResource SecondaryBtn}"/>
        </Grid>
    </Grid>
</Window>