<Window x:Class="Hospital.Desktop.Views.AbsentDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تفاصيل سجل الغياب" Height="320" Width="450" 
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft" ResizeMode="NoResize">

    <Grid Margin="25">
        <StackPanel>
            <Border BorderBrush="#E74C3C" BorderThickness="0,0,0,2" Margin="0,0,0,20" Padding="0,0,0,10">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="{Binding EmployeeName}" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                        <TextBlock Text="تفاصيل الغياب المسجل" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Id, StringFormat='رقم السجل: # {0}'}" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" Foreground="#7F8C8D"/>
                </Grid>
            </Border>

            <UniformGrid Columns="2">
                <StackPanel Margin="10">
                    <TextBlock Text="تاريخ الغياب" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Date, StringFormat='{}{0:yyyy-MM-dd}'}" FontSize="16" Margin="0,5"/>
                </StackPanel>

                <StackPanel Margin="10">
                    <TextBlock Text="حالة السجل" FontWeight="SemiBold"/>
                    <Border CornerRadius="5" Padding="8,4" HorizontalAlignment="Right" Margin="0,5">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDeleted}" Value="False">
                                        <Setter Property="Background" Value="#D5F5E3"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                        <Setter Property="Background" Value="#FADBD8"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDeleted}" Value="False">
                                            <Setter Property="Text" Value="فعال"/>
                                            <Setter Property="Foreground" Value="#1D8348"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                            <Setter Property="Text" Value="محذوف"/>
                                            <Setter Property="Foreground" Value="#943126"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </UniformGrid>

            <Button Content="إغلاق" IsCancel="True" Margin="0,30,0,0" Height="40" Width="120" Background="#95A5A6" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>