<Window x:Class="Hospital.Desktop.Views.DepartmentFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="بيانات القسم" Height="200" Width="350" WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="اسم القسم:"/>
            <TextBox Text="{Binding DepartmentName, UpdateSourceTrigger=PropertyChanged}" Margin="0,10" Padding="5"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="حفظ" Command="{Binding SaveCommand}" Width="100" Height="35" Background="#2ECC71" Foreground="White" BorderThickness="0"/>
                <Button Content="إلغاء" IsCancel="True" Width="100" Height="35" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>