<Window x:Class="Hospital.Desktop.Views.AddTransferView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إجراء نقل إداري" Height="450" Width="400" WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="البحث عن الموظف (بالاسم):" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding Employees}" DisplayMemberPath="Name" SelectedValuePath="Id" 
                      SelectedValue="{Binding Transfer.EmployeeId}" IsEditable="True" 
                      Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" StaysOpenOnEdit="True"/>

            <TextBlock Text="القسم الجديد:" Margin="0,15,0,5"/>
            <ComboBox ItemsSource="{Binding Departments}" DisplayMemberPath="Name" SelectedValuePath="Id" 
                      SelectedValue="{Binding Transfer.NewDepartmentId}"/>

            <TextBlock Text="الوجبة الجديدة:" Margin="0,15,0,5"/>
            <ComboBox ItemsSource="{Binding ShiftTypes}" SelectedItem="{Binding Transfer.NewShiftType}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource ShiftTypeConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="رقم الكتاب الإداري:" Margin="0,15,0,5"/>
            <TextBox Text="{Binding Transfer.AdOrderNumber}" Padding="5"/>

            <TextBlock Text="تاريخ النقل:" Margin="0,15,0,5"/>
            <DatePicker SelectedDate="{Binding Transfer.TransferDate, Converter={StaticResource DateOnlyConverter}}"/>

            <Button Content="تثبيت النقل وأرشفة الأمر" Command="{Binding SaveCommand}" Margin="0,30,0,0" 
                    Height="40" Background="#27AE60" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>